const addToCartButton  = document.querySelectorAll(".Content-add-to-cart");
let cartItems = document.querySelectorAll(".cart-list");
let cartTotal  = document.querySelectorAll(".cart-total-price");

 addToCartButton.forEach(button => {
    button.addEventListener("click", function(){
            
 });
 });